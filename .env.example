# Supported deployed combination: demo + mock only.
APP_MODE=demo
HUBSPOT_MODE=mock

# Required for demo sessions (server-only). Do not commit real values.
# Generate a local secret:
#   openssl rand -hex 32
# Minimum length: 32 characters. Required on Vercel Preview/Production.
DEMO_SESSION_SECRET=
